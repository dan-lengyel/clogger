apiVersion: v1
kind: Pod
metadata:
  name: clogger
spec:
  containers:
  - name: clogger
    image: c-logging:latest
    imagePullPolicy: Never
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
